# This file is autogenerated by shiftstack-qe automation
apiVersion: v1
baseDomain: "{{ cluster_name }}"
compute:
- name: worker
  platform:
    openstack:
      type: "{{ worker_flavor }}"
  replicas: {{ worker_replicas }}
controlPlane:
  name: master
  platform:
    openstack:
      type: "{{ master_flavor }}"
  replicas: {{ master_replicas }}
metadata:
  name: "{{ cluster_name }}"
networking:
  clusterNetworks:
  - cidr: 10.128.0.0/14
    hostSubnetLength: 9
  serviceNetwork:
    - 172.30.0.0/16
  machineNetwork:
    - cidr: "{{ machine_cidr }}"
  networkType: "{{ network_type }}"
platform:
  openstack:
    cloud:            "{{ user_cloud }}"
    externalNetwork:  "{{ external_network }}"
    region:           "{{ region }}"
    lbFloatingIP:     "{{ api_fip }}"
    ingressFloatingIP:     "{{ apps_fip }}"
    externalDNS:      {{ dns_servers }}
pullSecret: |
  {{ ocp_pull_secret }}
sshKey: |
  {{ ocp_public_key }}
